{
  "name": "newt",
  "version": "0.1.0",
  "private": true,
  "description": "Newt monorepo: a beginner-friendly DSL for Discord bots.",
  "workspaces": [
    "packages/compiler",
    "packages/cli"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "check": "npm run check --workspaces --if-present"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
